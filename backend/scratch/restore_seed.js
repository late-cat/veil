const fs = require('fs');
fs.writeFileSync('/Users/bapi/new-moon-stellar/backend/.midnight-state.json', JSON.stringify({
  walletSeed: "human oblige crater faint library cloth cherry volcano hill upon ticket sun forward insane discover token focus arrow give crouch sample screen thumb carbon"
}));
console.log('Seed phrase restored!');
